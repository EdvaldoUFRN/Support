# Support
This repository contains code created using PHP for the backend, the Bootstrap framework for styling, and SQL for querying the MYSQL database.
# Script to populate 
And I had also put a SQL script to populate my database and create its relational structure, or simply its tables.
